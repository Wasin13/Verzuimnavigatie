/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["localhost", "verzuimnavigator-api-a924da4d0ac1.herokuapp.com"],
    },
  };
  
  export default nextConfig;